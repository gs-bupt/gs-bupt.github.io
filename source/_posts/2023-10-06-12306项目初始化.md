---
title: 12306项目初始化
categories: 12306项目
excerpt: 12306 项目实战开篇：基于 Spring Boot 多模块开发初始化工程骨架，划分各模块职责。
date: 2023-10-06 16:47:53
tags:
- 12306
- 项目开发
---





### SpringBoot多模块开发

SpringBoot框架用作单体项目，可能不存在多模块的情况。但是可以在单体项目中使用多模块进行明确的职责划分。

如果是构建微服务项目，基本会对模块进行明确的划分，比如：

- 抽象定义公共代码及Util封装进行引用。
- 业务代码进行单独定义模块。
- 数据库等 DB 操作相关抽离单独模块。
- 提供外部平台调取的接口单独定义模块。
- ...

首先创建parent项目，修改项目的pom.xml文件

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
<!--    配置SpringBoot的父级项目   -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.1.3.RELEASE</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.lby</groupId>
    <artifactId>multi-module-demo</artifactId>
    <version>1.0-SNAPSHOT</version>
    <!--  packaging 父级项目的类型是pom  -->
    <!--  Jar： 内部调用或作为服务进行发布使用.  -->
    <!--  War： 需要部署的项目.  -->
    <!--  Pom：宏观而言即没有代码需要测试或者编译，也没有资源需要处理。寓意为一个父级项目，一般作为项目聚合和依赖传递使用. -->
    <packaging>pom</packaging>

    <!--   properties 定义pom的全局变量 一般用于定义依赖的版本号   -->
    <properties>
        <java.version>1.8</java.version>
        <lombok-version>1.18.4</lombok-version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <!--版本号通过$获取properties中定义的版本号-->
            <version>${lombok-version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <!--    dependencyManagement
        在 dependencyManagement 中配置的依赖 只是一种声明 声明了版本号
        不会被项目继承下来【相关的jar包不会被子项目下载到项目中】

        子项目如果想要继承到dependencyManagement中的依赖  需要单独在配置
        只不过子项目如果继承 dependencyManagement 中的依赖 可以不写版本号【子项目中依赖版本
        按照父项目中dependencyManagement 中配置下载】
    -->
    <dependencyManagement>
        <dependencies>
            <!--如果是Mybatis写成Mybatis即可-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>3.3.1.tmp</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
```

